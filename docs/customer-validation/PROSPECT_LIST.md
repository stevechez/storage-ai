# Prospect List — Sprint 17

This is a tracking template, not a pre-filled list — I don't have a way to know which independent storage facilities actually exist near you, so inventing names here would be worse than useless. Fill it in from real research (see "How to find them" in `IDEAL_CUSTOMER_PROFILE.md`), then update the status/notes columns as you make contact.

| Facility Name | Location | Contact | Source (how found) | Outreach Status | Discovery Call Date | Key Takeaway |
|---|---|---|---|---|---|---|
| | | | | Not contacted | | |
| | | | | Not contacted | | |
| | | | | Not contacted | | |
| | | | | Not contacted | | |
| | | | | Not contacted | | |

Outreach Status values to use consistently: `Not contacted` → `Messaged` → `Call scheduled` → `Call completed` → `Demo scheduled` → `Demo completed` → `No response` / `Not a fit`.

## Target for this sprint

Per the sprint's own success metrics: get to **first operator conversations scheduled**, not a fully worked list. A list of 10–15 real, researched facilities with 3–5 actually contacted is a realistic bar for one sprint — don't let building the list itself become the delay tactic for having the actual conversations.

## Running log of what you learn

Keep a running note here (or split into a separate file once it's substantial) of anything that should change what gets built next:

- _(add entries after each conversation)_
